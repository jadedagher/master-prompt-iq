{
  "buildCommand": "npm install && cd managed-chatkit && npm run frontend:build",
  "outputDirectory": "managed-chatkit/frontend/dist",
  "installCommand": "npm install && cd managed-chatkit && npm install && npm --prefix frontend install",
  "functions": {
    "api/**/*.js": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_OPTIONS": "--max-http-header-size=16384"
  }
}
